apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "course.fullname" . }}-config
  labels:
    {{- include "course.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "config/*").AsConfig | indent 2 }}
