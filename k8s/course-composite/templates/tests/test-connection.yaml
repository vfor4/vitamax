apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "course-composite.fullname" . }}-test-connection"
  labels:
    {{- include "course-composite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "course-composite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
