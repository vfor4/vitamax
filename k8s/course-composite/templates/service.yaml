apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-composite.fullname" . }}
  labels:
    {{- include "course-composite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "course-composite.selectorLabels" . | nindent 4 }}
