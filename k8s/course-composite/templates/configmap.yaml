apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "course-composite.fullname" . }}-config
  labels:
    {{- include "course-composite.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "config/*").AsConfig | indent 2 }}
